# Core CV / detection dependencies for the stronger line detectors
ultralytics>=8.0.0          # only needed if you still want YOLO fallback
Pillow>=10.0.0
numpy>=1.24.0

# Preferred for Muharaf
kraken>=5.0.0               # includes blla segmentation

# Strong modern alternative
surya-ocr>=0.6.0
rapidfuzz 
openai>=1.0.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
python-docx>=1.0.0
gradio>=4.0.0